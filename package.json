{
  "name": "statement-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.11.1",
  "engines": {
    "node": ">=22.16.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "deploy": "pnpm build && pnpm dlx wrangler pages deploy dist",
    "deploy:worker": "pnpm build && pnpm dlx wrangler deploy --config wragler.worker.toml",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.3.0",
    "astro": "^5.16.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
